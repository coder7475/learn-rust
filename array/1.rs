fn main() {
    // * declare array, initialize all values, compiler infers length = 7
    let days = ["Sunday", "Friday", "Saturday"];
    // access the days values
    let first = days[0];
    let second = days[1];
    // * Declare Array, initialize all values to 0, length = 5
    let bytes = [0; 5];

}